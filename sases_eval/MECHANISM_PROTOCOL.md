# Mechanism audit (retrospective)

Freeze these diagnostics before running them; retain the prior rule unchanged.
Use the saved 22-split, 12-anchor sweep and canonical configurations. Reuse
saved model defects and errors; model training is unnecessary for a law audit.

1. Ablate boundary-only, fitted slope + boundary, theory slope alone, theory
   slope + boundary. Compare coverage, minimum margin, usefulness and mean S/e.
2. Check the vector identity r(u)=r(u0)+(u-u0)*(q_high-q_secant).
3. Audit the necessary endpoint condition norm(q_high-q_secant)<=k, and the
   stronger observable check on every consecutive measured-power interval.
   Passing secants cannot establish a continuous derivative bound between powers.
4. Measure vector cancellation and triangle-inequality slack. This can explain
   endpoint coverage even if a proposed growth assumption fails.
5. Fixed reference-slope controls q_ref=0 and q_ref=1, alongside q_ref=0.5.
   Do not select a new reference exponent using test coverage. Equal success
   would weaken attribution to the specific KZ exponent.
6. A deterministic constructed curve with a hidden sinusoidal excursion tests
   whether successful endpoint audits can conceal an interior failure. This is
   a mathematical stress control, not extra physical validation data.

Report raw rows and effective trial counts. All this corpus has already been
viewed. No audit row or synthetic example adds independent apparatus trials.
